<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AppTFG.Paginas.Registrarse"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:renderers="clr-namespace:AppTFG.Renderers"
    NavigationPage.HasNavigationBar="False"
    xmlns:pv="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    BackgroundColor="#791AE5"
    Padding="0,40,0,0">
    <ScrollView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <StackLayout >
            <StackLayout  Margin="0,0,0,0"  Orientation="Horizontal">
                <StackLayout Margin="20,10,20,0" Spacing="20">
                    <ImageButton HeightRequest="30" HorizontalOptions="Start" Source="close" VerticalOptions="Start"
                    WidthRequest="30" Command="{Binding Login}" BackgroundColor="Transparent"/>
                    <Label FontAttributes="Bold" FontSize="30" Text="Crea tu cuenta" TextColor="White"
                    VerticalOptions="CenterAndExpand" />
                </StackLayout>
                <Image Source="bgImage1" WidthRequest="200" />
            </StackLayout>
            <pv:PancakeView Margin="0,20,0,0" Padding="30,50,30,30" BackgroundColor="White" CornerRadius="40,40,0,0"
            VerticalOptions="FillAndExpand">
                <StackLayout Spacing="15">
                    <StackLayout Spacing="10">
                        <Label Margin="20,0,0,0" FontSize="20" Text="Email" TextColor="#791AE5" />
                        <pv:PancakeView Padding="10,0,0,0" BackgroundColor="White" BorderThickness="1"
                        CornerRadius="15" Elevation="3" HeightRequest="50">
                            <renderers:BorderlessEntry FontSize="20" Placeholder="Email" PlaceholderColor="#99A1B3" 
                            Text="{Binding Nombre}" ClearButtonVisibility="WhileEditing" Keyboard="Email"
                            TextColor="#484848" />
                        </pv:PancakeView>
                    </StackLayout>
                    <StackLayout Spacing="10">
                        <Label Margin="20,0,0,0" FontSize="20" Text="Contraseña" TextColor="#791AE5" />
                        <pv:PancakeView Padding="10,0,0,0" BackgroundColor="White" BorderThickness="1"
                        CornerRadius="15" Elevation="3" HeightRequest="50">
                            <renderers:BorderlessEntry FontSize="20" Placeholder="Contraseña" PlaceholderColor="#99A1B3" 
                            Text="{Binding Password}" ClearButtonVisibility="WhileEditing" IsPassword="True" 
                            TextColor="#484848" />
                        </pv:PancakeView>
                    </StackLayout>
                    <StackLayout Spacing="10">
                        <Label Margin="20,0,0,0" FontSize="20" Text="Confirmar Contraseña" TextColor="#791AE5" />
                        <pv:PancakeView Padding="10,0,0,0" BackgroundColor="White" BorderThickness="1"
                        CornerRadius="15" Elevation="3" HeightRequest="50">
                            <renderers:BorderlessEntry FontSize="20" Placeholder="Introduce contraseña anterior" 
                            PlaceholderColor="#99A1B3" Text="{Binding ConfirmPassword}" 
                            ClearButtonVisibility="WhileEditing" IsPassword="True" TextColor="#484848" />
                        </pv:PancakeView>
                    </StackLayout>
                    <pv:PancakeView BackgroundColor="#791AE5" BorderThickness="1" CornerRadius="25"
                    Elevation="3" HeightRequest="50" HorizontalOptions="CenterAndExpand" WidthRequest="175">
                        <Button FontAttributes="Bold" FontSize="Medium" HorizontalOptions="CenterAndExpand"
                        Text="Crear usuario" TextColor="White" VerticalOptions="CenterAndExpand" 
                        Command="{Binding SignUpCommand}" BackgroundColor="Transparent"/>
                    </pv:PancakeView>
                </StackLayout>
            </pv:PancakeView>
        </StackLayout>
    </ScrollView>
</ContentPage>
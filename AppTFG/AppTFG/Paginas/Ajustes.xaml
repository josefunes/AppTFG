<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppTFG.Paginas.Ajustes"
             Title="Ajustes">
    <ContentPage.Content>
        <StackLayout BackgroundColor="White" HorizontalOptions="FillAndExpand">
            <StackLayout BackgroundColor="White" Orientation="Vertical" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <!--<Label Text="Para cambiar su contraseña primero introduzca la contraseña actual:"/>
                <Entry Text="{Binding Password}" IsPassword="True" Margin="10, 10" Placeholder="Inserte su contraseña actual"/>
                <Entry Text="{Binding Password}" IsPassword="True" Margin="10, 10" Placeholder="Repita su contraseña actual"/>-->
                <Entry Text="{Binding Password}" IsPassword="True" Margin="10, 10" Placeholder="Inserte nueva contraseña"/>
                <!--<Button x:Name="updatebtn" Text="Change Password" Command="{Binding UpdateCommand}" TextColor="Black" BackgroundColor="White" HorizontalOptions="Center"/>-->
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
